sctab
=====

Linux system call table.

Building
--------

Assuming `dub` is installed:

    ./build.sh

Generating
----------

Run `-f` flag first to pull neccessary files:
    
    ./sctab -f

Then, execute:

    ./sctab
