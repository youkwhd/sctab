sctab
=====

Linux system call table.

Building
--------

Assuming `dub` is installed:

    ./build.sh

Generating
----------

Run `./sctab --fetch` first to pull neccessary files,
then execute:

    ./sctab
