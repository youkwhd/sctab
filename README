sctab
=====

Linux system call table.

Building
--------

Assuming `dub` is installed:

    ./build.sh

Generating
----------

Run `./sctab -f` first to pull neccessary files,
then execute:

    ./sctab
